language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
install:
  - "pip install -r requirements.txt"
  - "pip install -r coveralls"
script:
  - "nosetests --with-coverage --cover-package=."
after_success:
  - "coveralls"
